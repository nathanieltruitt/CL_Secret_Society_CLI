# frozen_string_literal: true

require 'bcrypt'

require_relative "file_encrypter_cli/version"

module FileEncrypterCli
  class Error < StandardError; end
  # Encrypter handles the create of the password file as well as generate the hashed passwords

end
